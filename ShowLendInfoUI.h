#pragma once

#include <vector>
#include <string>
#include <utility>

using std::vector;
using std::string;
using std::pair;

class ShowLendInfoUI {
public:
    static vector<pair<string, string>> handleShowLendInfo();
};
